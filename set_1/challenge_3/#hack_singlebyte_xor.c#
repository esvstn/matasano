#include <string.h>
#include <stdio.h>
#include "werror.h"

unsigned char* countscore(unsigned char* string, )
{
 
}

			  int main(int argc, unsigned char* argv[])
{
  if( argc < 1 )
    {
      werror("no input file");
      return -1;
    }

  
